#!/bin/bash

name=${1:-wasabi}
user=${2:-ubuntu}

echo -n "Obtaining the public DNS of $name ..."
host=$(./wasabi-hostname $name)
echo "Done! : $host"

echo "Running post_install manually ..."

cat post_install.sh | ssh $user@$host sudo bash

echo "Logging in to $user@$host ..."

ssh $user@$host
